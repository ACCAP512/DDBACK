"""API layer — FastAPI routers for the broker OS. In later milestones this supersedes the single
``api/main.py`` (M3 adds auth + portfolio/client/claim/docs routers). M0 ships a minimal app
(health + readiness) so the layer exists and is wired to the DB.
"""
